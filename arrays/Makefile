.PHONY: clean All

All:
	@echo "----------Building project:[ max_sum_increasing_subsequence - Debug ]----------"
	@cd "max_sum_increasing_subsequence" && "$(MAKE)" -f  "max_sum_increasing_subsequence.mk"
clean:
	@echo "----------Cleaning project:[ max_sum_increasing_subsequence - Debug ]----------"
	@cd "max_sum_increasing_subsequence" && "$(MAKE)" -f  "max_sum_increasing_subsequence.mk" clean
