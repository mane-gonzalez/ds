.PHONY: clean All

All:
	@echo "----------Building project:[ max_sum - Debug ]----------"
	@cd "max_sum" && "$(MAKE)" -f  "max_sum.mk"
clean:
	@echo "----------Cleaning project:[ max_sum - Debug ]----------"
	@cd "max_sum" && "$(MAKE)" -f  "max_sum.mk" clean
